import { ERROR_RED } from '../../../utils/constants'

export const calendarClassess = {
  formError: {
    color: ERROR_RED,
    fontSize: '12px',
    fontWeight: 600,
    lineHeight: '20px',
    marginBottom: '4px',
  },
}
