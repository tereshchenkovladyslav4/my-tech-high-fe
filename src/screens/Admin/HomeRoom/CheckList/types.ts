export type SubjectCheckList = {
  id: number
  grade: string
  subject: number
  goal?: string[]
  schoolYear?: number
  // Temp fields
}

export type IndependentCheckList = {
  id: number
  grade: string
  subject: number
  goal?: string[]
  schoolYear?: number
  // Temp fields
}
