import React from 'react'
import { FunctionComponent } from 'react'
import { Card as MuiCard } from '@mui/material'

export const Card: FunctionComponent = ({ children }) => <MuiCard>{children}</MuiCard>
