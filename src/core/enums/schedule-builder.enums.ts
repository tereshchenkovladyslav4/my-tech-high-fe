export enum ScheduleBuilder {
  MAX_NUM_PERIODS = 'Max Number of Periods',
  PARTY_PROVIDER = '3rd Party Provider',
  CUSTOM_BUILT = 'Custom-built',
  SPLIT_ENROLLMENT = 'Split Enrollment',
  TOOLTIP = 'Tooltip for Parents',
  CHECKBOX_LABEL = 'Always unlock for 2nd Semester',
}
