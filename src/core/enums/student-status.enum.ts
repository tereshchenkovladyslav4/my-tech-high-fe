export enum StudentStatus {
  PENDING = 0,
  ACTIVE = 1,
  WITHDRAWN = 2,
  GRADUATED = 3,
  APPLIED = 5,
  REAPPLIED = 7,
  ACCEPTED = 6,
  /**
   * Packet is deleted by Admin
   */
  DELETED = 7,
}
